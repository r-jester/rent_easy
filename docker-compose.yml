version: '3.8'

services:
  flutter_app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    environment:
      - FLUTTER_WEB=true
    command: flutter run -d web --web-port=8080
